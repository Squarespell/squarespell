# Internal-only gateway (never published). supabase-js calls <SUPABASE_URL>/rest/v1/...;
# PostgREST serves the same routes without the /rest/v1 prefix, so the prefix is stripped here.
{
	auto_https off
	admin off
}

:3100 {
	handle_path /rest/v1* {
		reverse_proxy rest:3000
	}
	respond 404
}
