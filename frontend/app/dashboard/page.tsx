'use client'; import { useEffect, useState } from 'react'; import { useAuth } from '@clerk/nextjs'; import { useRouter } from 'next/navigation'; const API = process.env.NEXT_PUBLIC_API_URL || 'https://squarespell-backend.onrender.com'; const ACC = '#D2FF1D'; export default function Dashboard() { const { getToken } = useAuth(); const router = useRouter(); const [src, setSrc] = useState(''); const [status, setStatus] = useState<'loading'|'trial'|'active'|'expired'>('loading'); const [daysLeft, setDaysLeft] = useState(0); const [retrying, setRetrying] = useState(false); useEffect(() => { async function fetchMe(token: string, isRetry = false) { const ctrl = new AbortController(); const timeout = setTimeout(() => ctrl.abort(), 30000); try { const res = await fetch(`${API}/api/user/plan`, { headers: { Authorization: `Bearer ${token}` }, signal: ctrl.signal }); clearTimeout(timeout); const data = await res.json(); const plan = data.plan || 'trial'; const trialEnds = data.trial_ends_at ? new Date(data.trial_ends_at) : null; const now = new Date(); if (plan !== 'trial') { setStatus('active'); } else if (trialEnds && now > trialEnds) { setStatus('expired'); } else { const days = trialEnds ? Math.ceil((trialEnds.getTime() - now.getTime()) / 86400000) : 7; setDaysLeft(days); setStatus('trial'); } setRetrying(false); setSrc(`/squarespell-app.html?t=${encodeURIComponent(token)}`); } catch { clearTimeout(timeout); if (!isRetry) { setRetrying(true); await new Promise(r => setTimeout(r, 5000)); fetchMe(token, true); } else { setRetrying(false); setStatus('expired'); } } } getToken().then(token => { if (token) fetchMe(token); }); }, []); if (status === 'loading') return ( <div style={{background:'#07090c',height:'100vh',display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',gap:'16px',fontFamily:'DM Sans,system-ui,sans-serif'}}> <div style={{width:'32px',height:'32px',border:'2px solid rgba(210,255,29,.2)',borderTopColor:ACC,borderRadius:'50%',animation:'spin .7s linear infinite'}}/> {retrying && <p style={{fontSize:'14px',color:'rgba(240,242,245,.5)',margin:0}}>Connecting to server...</p>} <style>{`@keyframes spin{to{transform:rotate(360deg)}}`}</style> </div> ); if (status === 'expired') return ( <div style={{minHeight:'100vh',background:'#07090c',display:'flex',alignItems:'center',justifyContent:'center',fontFamily:'"DM Sans",system-ui,sans-serif'}}> <div style={{maxWidth:'480px',width:'100%',padding:'48px 32px',textAlign:'center'}}> <div style={{display:'flex',alignItems:'center',justifyContent:'center',gap:'10px',marginBottom:'40px'}}> <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#D2FF1D" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"><polyline points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg> <span style={{fontSize:'20px',fontWeight:800,color:'#f0f2f5',letterSpacing:'-.03em'}}>Squarespell</span> </div> <h1 style={{fontSize:'32px',fontWeight:800,color:'#f0f2f5',letterSpacing:'-.04em',marginBottom:'12px'}}>Your free trial has ended</h1> <p style={{fontSize:'16px',color:'rgba(240,242,245,0.5)',lineHeight:'1.6',marginBottom:'40px'}}>Upgrade to keep access to your leads and quizzes. Your data is safe.</p> <div style={{display:'flex',flexDirection:'column',gap:'12px',marginBottom:'32px'}}> <a href="/pricing" style={{display:'block',padding:'14px',background:'rgba(255,255,255,0.04)',border:'1px solid rgba(255,255,255,0.08)',borderRadius:'10px',color:'#f0f2f5',textDecoration:'none',fontSize:'15px',fontWeight:600}}>Starter — $19/mo</a> <a href="/pricing" style={{display:'block',padding:'14px',background:ACC,border:'none',borderRadius:'10px',color:'#07090c',textDecoration:'none',fontSize:'15px',fontWeight:700}}>Pro — $39/mo — Recommended</a> <a href="/pricing" style={{display:'block',padding:'14px',background:'rgba(255,255,255,0.04)',border:'1px solid rgba(255,255,255,0.08)',borderRadius:'10px',color:'#f0f2f5',textDecoration:'none',fontSize:'15px',fontWeight:600}}>Agency — $79/mo</a> </div> <p style={{fontSize:'14px',color:'rgba(240,242,245,0.3)'}}>Questions? <a href="mailto:hello@squarespell.com" style={{color:'rgba(240,242,245,0.5)',textDecoration:'none'}}>hello@squarespell.com</a></p> </div> </div> ); return ( <> {status === 'trial' && daysLeft > 3 && daysLeft <= 10 && ( <div style={{background:'#1a1f2e',borderBottom:'1px solid rgba(255,255,255,0.1)',padding:'14px 24px',display:'flex',alignItems:'center',justifyContent:'space-between',fontFamily:'DM Sans,system-ui,sans-serif',flexWrap:'wrap',gap:'12px'}}> <span style={{fontSize:'15px',color:'rgba(240,242,245,0.85)'}}> Your trial expires in <strong style={{color:'#f0f2f5'}}>{daysLeft} days</strong>. Add a payment method to ensure uninterrupted service when your trial expires. </span> <button onClick={() => router.push('/pricing')} style={{background:'#f0f2f5',color:'#07090c',border:'none',borderRadius:'8px',padding:'10px 20px',fontSize:'14px',fontWeight:700,cursor:'pointer',whiteSpace:'nowrap'}}> Add a Card </button> </div> )} {status === 'trial' && daysLeft <= 3 && ( <div style={{background:'rgba(210,255,29,.08)',borderBottom:'1px solid rgba(210,255,29,.2)',padding:'10px 24px',display:'flex',alignItems:'center',justifyContent:'space-between',fontFamily:'DM Sans,system-ui,sans-serif',flexWrap:'wrap',gap:'8px'}}> <span style={{fontSize:'14px',color:'rgba(240,242,245,.75)'}}> ⚡ <strong style={{color:ACC}}>{daysLeft} day{daysLeft !== 1 ? 's' : ''} left</strong> on your free trial </span> <button onClick={() => router.push('/pricing')} style={{background:ACC,color:'#07090c',border:'none',borderRadius:'8px',padding:'7px 18px',fontSize:'13px',fontWeight:700,cursor:'pointer'}}> Upgrade now </button> </div> )} {src && ( <iframe src={src} style={{width:'100%',height:status==='trial'&&daysLeft<=3?'calc(100vh - 45px)':'100vh',border:'none',display:'block'}} title="Squarespell" /> )} </> ); }
